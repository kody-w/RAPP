# RAPP Swarm — legacy Tier-2 retired

**HTTP status: 410 Gone.**

The retained `function_app.py` is a non-executable historical tombstone, not a
RAPP/1 deployment target. Packaging and provisioning fail closed; the
`.funcignore` deployment manifest excludes the entrypoint.

For canonicalization, identity, frames, wire, eggs, registry, trust, and
protocol evolution, follow RAPP/1 rev-5 through
[`RAPP1_AUTHORITY.json`](../RAPP1_AUTHORITY.json) and
[`RAPP1_STATUS.md`](../RAPP1_STATUS.md). See
`RAPP1_DEPLOYMENT_GUARD.json` for the local containment evidence.
