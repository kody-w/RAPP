# RAPP Swarm — legacy Tier-2 retired

**HTTP status: 410 Gone.**

The retained `function_app.py` is historical implementation material and is
not a RAPP/1 deployment target. Packaging and provisioning fail closed; the
`.funcignore` deployment manifest excludes the runtime entrypoint.

See `RAPP1_DEPLOYMENT_GUARD.json` and, for restoration requirements,
`../RAPP1_CONFORMANCE.md`.
