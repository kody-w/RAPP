# utils — shared modules every rapplication singleton may import inside a
# running brainstem (utils.llm, utils.bond). Keep this package tiny: a
# singleton must stay a single droppable file; these are the host's organs,
# not the rapp's.
