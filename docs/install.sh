#!/usr/bin/env bash
cat >&2 <<'EOF'
docs/install.sh: 410 Gone

This legacy passthrough is retired because it selected mutable upstream bytes.
The only active Unix installer is ../installer/install.sh.
EOF
exit 78
